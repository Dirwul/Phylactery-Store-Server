spring:
  application:
    name: PhylacStoreServer
  datasource:
    url: jdbc:postgresql://localhost:5432/phylactery_db
    username: dirwul
    password: ${DB_PASS}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false
    properties:
      hibernate.transaction.jta.platform: org.hibernate.engine.transaction.jta.platform.internal.NoJtaPlatform
